{
  "name": "aider-rate-limit-proxy",
  "version": "1.0.0",
  "description": "Fixed-minute-window rate-limiting proxy: Aider → Nvidia API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
